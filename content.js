// Content script runs on all pages
// It can be used for future enhancements like highlighting base64 params in URLs
// For now, it's just a placeholder as the main functionality is in the popup

console.log('Base64 Query Inspector loaded');

// Future enhancement: Could add visual indicators for pages with base64 params
// or inject UI elements directly into the page